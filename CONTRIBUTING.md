# Contributing to Sanctuary

1. All code changes must align with the architecture documents in `docs/`.
2. Ensure you run `npm run lint` and `npm run test` before submitting changes.
3. This is a broadcast-critical application. Performance and stability take priority over new features.
4. UI changes must adhere to the Design System (`src/renderer/styles/design-system.css`).
